// Re-export of the parent's tool: the task child runs the coder's full tool
// surface (a declared subagent inherits nothing, so parity is by re-export).
export { default } from "../../../tools/tasks";
