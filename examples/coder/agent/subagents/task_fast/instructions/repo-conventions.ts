import { stdlib } from "../../../lib/stdlib";

// Root AGENTS.md for the task child — declared subagents inherit no
// instructions, and a worker without the project conventions edits a
// documented project without its rules.
export default stdlib.instructions.repoConventions;
