import { stdlib } from "../../../lib/stdlib";

// The how-to-work contract — same SDK prose as the parent, re-exported
// because declared subagents inherit nothing.
export default stdlib.instructions.workflow;
