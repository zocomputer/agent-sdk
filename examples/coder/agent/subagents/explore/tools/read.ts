import { exploreTools } from "../lib/explore-tools";

// The explore child's file reader (windowed text + PDF/DOCX/spreadsheet
// extraction, dir-conventions riders; images degrade to metadata — no
// park-delivery hook in the child).
export default exploreTools.read;
