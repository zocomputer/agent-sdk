import { stdlib } from "../lib/stdlib";

// Workflow guidance for the async task tools. eve auto-loads instruction files
// the same way it loads tools.
export default stdlib.instructions.parallelTools;
