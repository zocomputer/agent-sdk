import { stdlib } from "../lib/stdlib";

// The how-to-work contract: explore before edit, read before edit, verify,
// todo tracking, and the end-of-turn completeness check.
export default stdlib.instructions.workflow;
