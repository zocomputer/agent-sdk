import { stdlib } from "../lib/stdlib";

// The ask_question playbook: eve ships the HITL tool with a one-line
// description; this teaches its structured surface (options, styles,
// allowFreeform) and when to ask at all.
export default stdlib.instructions.hitl;
